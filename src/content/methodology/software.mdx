import { AppLink } from '@/components'
import { FontAwesomeIcon } from '@fortawesome/react-fontawesome'
import { faGithub, faGitlab } from '@fortawesome/free-brands-svg-icons'

export const softwareRepos = [
  {
    blockchain: 'Bitcoin',
    clients: [
      { name: 'Bitcoin Core', repo: 'bitcoin/bitcoin', url: 'https://github.com/bitcoin/bitcoin' }
    ]
  },
  {
    blockchain: 'Bitcoin Cash',
    clients: [
      { name: 'Bitcoin Cash Node', repo: 'bitcoin-cash-node/bitcoin-cash-node', url: 'https://github.com/bitcoin-cash-node/bitcoin-cash-node' },
      { name: 'Bitcoin Unlimited', repo: 'bitcoinunlimited/BCHUnlimited', url: 'https://gitlab.com/bitcoinunlimited/BCHUnlimited' }
    ]
  },
  {
    blockchain: 'Cardano',
    clients: [
      { name: 'Cardano Node', repo: 'IntersectMBO/cardano-node', url: 'https://github.com/IntersectMBO/cardano-node' }
    ]
  },
  {
    blockchain: 'Ethereum (execution clients)',
    clients: [
      { name: 'Go Ethereum', repo: 'ethereum/go-ethereum', url: 'https://github.com/ethereum/go-ethereum' },
      { name: 'Nethermind', repo: 'NethermindEth/nethermind', url: 'https://github.com/NethermindEth/nethermind' },
      { name: 'Besu', repo: 'hyperledger/besu', url: 'https://github.com/hyperledger/besu' },
      { name: 'Erigon', repo: 'ledgerwatch/erigon', url: 'https://github.com/ledgerwatch/erigon' },
      { name: 'Reth', repo: 'paradigmxyz/reth', url: 'https://github.com/paradigmxyz/reth' }
    ]
  },
  {
    blockchain: 'Ethereum (consensus clients)',
    clients: [
      { name: 'Lighthouse', repo: 'sigp/lighthouse', url: 'https://github.com/sigp/lighthouse' },
      { name: 'Lodestar', repo: 'ChainSafe/lodestar', url: 'https://github.com/ChainSafe/lodestar' },
      { name: 'Nimbus', repo: 'status-im/nimbus-eth2', url: 'https://github.com/status-im/nimbus-eth2' },
      { name: 'Prysm', repo: 'OffchainLabs/prysm', url: 'https://github.com/OffchainLabs/prysm' },
      { name: 'Teku', repo: 'Consensys/teku', url: 'https://github.com/Consensys/teku' },
      { name: 'Grandine', repo: 'grandinetech/grandine', url: 'https://github.com/grandinetech/grandine' }
    ]
  },
  {
    blockchain: 'Litecoin',
    clients: [
      { name: 'Litecoin Core', repo: 'litecoin-project/litecoin', url: 'https://github.com/litecoin-project/litecoin' }
    ]
  },
  {
    blockchain: 'Polkadot',
    clients: [
      { name: 'Polkadot SDK', repo: 'paritytech/polkadot-sdk', url: 'https://github.com/paritytech/polkadot-sdk' }
    ]
  },
  {
    blockchain: 'Solana',
    clients: [
      { name: 'Solana', repo: 'solana-labs/solana', url: 'https://github.com/solana-labs/solana' }
    ]
  },
  {
    blockchain: 'Tezos',
    clients: [
      { name: 'Tezos', repo: 'tezos/tezos', url: 'https://gitlab.com/tezos/tezos' }
    ]
  },
  {
    blockchain: 'XRPL',
    clients: [
      { name: 'rippled', repo: 'XRPLF/rippled', url: 'https://github.com/XRPLF/rippled' }
    ]
  },
  {
    blockchain: 'Zcash',
    clients: [
      { name: 'Zcash', repo: 'zcash/zcash', url: 'https://github.com/zcash/zcash' },
      { name: 'Zebra', repo: 'ZcashFoundation/zebra', url: 'https://github.com/ZcashFoundation/zebra' }
    ]
  }
]

On the software layer, decentralisation is evaluated by looking at the open source repositories associated with different blockchain clients, and specifically looking at their commit history. Specifically, the following metrics are presented in the dashboard: Shannon entropy, Herfindahl-Hirschman index (HHI), 1-concentration ratio, number of total entities.

For blockchains that are implemented by more than one independent client, the commit histories of all of their client repositories are merged into a single, chronologically ordered history before any metrics are calculated, so that decentralisation is evaluated at the level of the blockchain as a whole rather than per individual client. Note that Ethereum's execution and consensus clients are treated as two separate systems for this purpose.

The table below lists the projects that are currently supported.

<div className="rounded-xl border border-base-300 overflow-hidden bg-base-100 shadow-sm">
  <div className="px-4 py-2.5 bg-base-200/50 border-b border-base-300">
    <span className="text-xs font-semibold text-base-content/70 uppercase tracking-[0.12em]">Supported Repositories</span>
  </div>
  <div className="overflow-x-auto">
    <table className="table table-sm [&_th]:align-top [&_td]:align-top">
      <thead>
        <tr>
          <th className="text-[10px] font-mono font-semibold text-base-content/50 uppercase tracking-wider">Blockchain</th>
          <th className="text-[10px] font-mono font-semibold text-base-content/50 uppercase tracking-wider">Client</th>
          <th className="text-[10px] font-mono font-semibold text-base-content/50 uppercase tracking-wider">Repository</th>
        </tr>
      </thead>
      <tbody>
        {softwareRepos.map((group, groupIdx) =>
          group.clients.map((client, i) => (
            <tr
              key={`${group.blockchain}-${client.name}`}
              className={`${groupIdx % 2 === 1 ? 'bg-base-200/40' : ''}${
                i === 0 && groupIdx > 0 ? ' border-t-2 border-base-300' : ''
              }`}
            >
              {i === 0 && (
                <td rowSpan={group.clients.length} className="font-medium text-base-content/80">
                  {group.blockchain}
                </td>
              )}
              <td>
                <span className="badge badge-outline badge-sm font-normal text-base-content/70 whitespace-nowrap">
                  {client.name}
                </span>
              </td>
              <td>
                <AppLink
                  href={client.url}
                  className="inline-flex items-center gap-1.5 no-underline hover:underline"
                >
                  <FontAwesomeIcon
                    icon={client.url.includes('gitlab.com') ? faGitlab : faGithub}
                    className="text-base-content/40 shrink-0"
                  />
                  <span className="font-mono text-xs">{client.repo}</span>
                </AppLink>
              </td>
            </tr>
          ))
        )}
      </tbody>
    </table>
  </div>
</div>

Users can choose which options will be taken into account when generating the graphs.

<div className="rounded-xl border border-base-300 overflow-hidden bg-base-100 shadow-sm">
  <div className="px-4 py-2.5 bg-base-200/50 border-b border-base-300">
    <span className="text-xs font-semibold text-base-content/70 uppercase tracking-[0.12em]">Filter Options</span>
  </div>
  <div className="divide-y divide-base-200">
    <div className="px-4 py-3 space-y-2">
      <p className="text-sm font-semibold text-base-content">Contribution Type</p>
      <p className="text-xs text-base-content/50">What counts as a contribution</p>
      <ul className="list-disc pl-5 text-sm text-base-content/70 space-y-1 mt-1">
        <li>
          <span className="font-medium text-base-content/90">Commits</span> — each commit is counted as a single
          contribution.
        </li>
        <li>
          <span className="font-medium text-base-content/90">Merge commits</span> — only merge commits are counted as
          contributions.
        </li>
        <li>
          <span className="font-medium text-base-content/90">Lines changed</span> — each line of code changed (added or
          removed) counts as a contribution; all commits are counted but each is weighted by the number of updated
          lines.
        </li>
      </ul>
    </div>
    <div className="px-4 py-3 space-y-2">
      <p className="text-sm font-semibold text-base-content">Contributor Type</p>
      <p className="text-xs text-base-content/50">Who is seen as responsible for the contribution</p>
      <ul className="list-disc pl-5 text-sm text-base-content/70 space-y-1 mt-1">
        <li>
          <span className="font-medium text-base-content/90">Author</span> — the person who wrote the code of the
          commit.
        </li>
        <li>
          <span className="font-medium text-base-content/90">Committer</span> — the person who committed the code on
          behalf of the original author (can be the same or different).
        </li>
      </ul>
    </div>
    <div className="px-4 py-3 space-y-2">
      <p className="text-sm font-semibold text-base-content">Commits per Sample Window</p>
      <p className="text-xs text-base-content/50">How many consecutive commits to group before applying a metric</p>
      <div className="flex flex-wrap gap-2 pt-1">
        {['100', '250', '500', '1000'].map((v) => (
          <span
            key={v}
            className="px-2.5 py-1 rounded-lg bg-base-200 text-xs font-mono font-semibold text-base-content/60"
          >
            {v}
          </span>
        ))}
      </div>
    </div>
  </div>
</div>

In the 'Client Distribution' part, decentralisation is also evaluated by looking at the distribution of the blockchain clients themselves. With the exception of Cardano, the blockchain data that is needed to determine these distributions are collected using crawlers running on each network. The results are based only on data we have collected and do not include extensive historical data.

For the Bitcoin, Bitcoin Cash, Litecoin and Zcash networks, the crawler we use is based on the <AppLink href="https://bitnodes.io/">Bitnodes</AppLink> project.

In Ethereum's case, the network architecture consists of two distinct layers: execution and consensus. The dashboard reflects this architectural separation. The crawler used is <AppLink href="https://github.com/cskiraly/fast-ethereum-crawler">fast-ethereum-crawler</AppLink>.

Finally, for Cardano, the official documentation refers to only one client implementation, and the dashboard therefore displays it. Several other clients are in development, but not ready for production yet: <AppLink href="https://github.com/blinklabs-io/dingo">Dingo</AppLink>, <AppLink href="https://github.com/harmoniclabs/gerolamo">Gerolamo</AppLink>, <AppLink href="https://github.com/pragma-org/amaru">Amaru</AppLink>, <AppLink href="https://github.com/txpipe/dolos">Dolos</AppLink>.
